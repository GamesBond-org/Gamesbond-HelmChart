apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gamesbond-dev
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/GamesBond-org/Gamesbond-HelmChart.git
    targetRevision: develop
    path: umbrella
    helm:
      valueFiles:
        - values.yaml
        - devvalues.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true