apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gamesbond-redis-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/yourusername/Gamesbond-HelmChart
    targetRevision: main
    path: templates/redis
    helm:
      values: |
        namespace: prod
        redis:
          enabled: true
          replicas: 1
      valueFiles:
      - values.yaml
      - prodvalues.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
